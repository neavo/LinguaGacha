[project]
name = "linguagacha"
version = "0.41.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.12.*"
dependencies = [
    # System
    "PyQt-Fluent-Widgets[full]",
    "openai",
    "anthropic",
    "google-genai",
    "tiktoken",
    "opencc",
    "openpyxl",
    "lxml",
    "beautifulsoup4",

    # Tools
    "rich",
    "httpx",
    "json-repair",
    "charset-normalizer",
    "zstandard",
]

[tool.uv]
override-dependencies = [
    "pyqt5-qt5==5.15.18; sys_platform != 'win32'",
    "pyqt5-qt5==5.15.2; sys_platform == 'win32'",
]

[tool.ruff]
force-exclude = true        # 强制应用排除规则
extend-exclude = [
    "module/Localizer/",
]

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint.isort]
force-single-line = true