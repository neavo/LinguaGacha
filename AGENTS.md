# LinguaGacha Agent 协作指南

本文件是仓库内始终适用的行动规则，与项目工程文档共同约束维护工作。当前任务和会话中已有的授权决定执行范围；仓库材料之间的行动要求冲突时以本文件为准，工程描述与实现冲突时以当前代码事实为准。

## 1. 任务边界

- 审查、解释、诊断、计划和明确只读的任务只读取、验证并报告；用户要求修改时完成实现、相关验证、diff 检查和交付，遇到阻塞先自行排查。
- 根据当前请求与会话已有授权判断下一步。外部写入、破坏性操作、付费行为或实质扩大范围尚未获得授权时，在完成已授权且可供审阅的准备工作后请求确认。
- 以本次需求及其影响范围内的整体方案为单位，优先降低代码的长期维护成本和结构理解成本；改动行数与文件数量不作为方案优劣的主要依据。
- 从真实责任入口解决问题。重复逻辑、错位职责或失效结构影响本次需求时，主动重构、合并或删除，并同步调整受影响的调用者与契约。
- 新增抽象、防御逻辑和测试应对应当前需求、真实边界或已确认风险；优先复用已有能力与测试覆盖，使实现和验证规模与问题相称。
- 兼容层须有明确的兼容要求且得到用户的授权。
- 保留用户和其他 Agent 的无关改动，编辑及自动修复只覆盖本任务文件。
- 已有上下文足够时直接推进；需要补齐项目背景、跨层事实或验证入口时，按 [WORKFLOW](docs/WORKFLOW.md) 选择相关材料。

## 2. 工程约束

### 状态与边界

- 改共享状态、任务、文件集合、数据库或 `.lg` 写入前，确认状态拥有者与唯一写入口。
- 涉及事件传播时检查快照形状和事件回流，涉及并发时检查互斥，涉及持久化或多步写入时检查事务、失败回滚和补偿语义。
- 新增状态前明确它属于后端运行态、数据库事实、前端共享运行态、页面缓存还是页面本地状态。
- 跨层、跨线程、跨前后端载荷使用显式类型、窄化函数或不可变值对象，不传可变引用。
- 改变可观察行为或机器契约边界时，先确认现有测试能否证明变化；存在覆盖缺口时补行为测试。无法验证的部分说明依据与影响。

### 实现与测试组织

- 优先使用通俗语言而非行话，沿用所在领域的既有词表，同一业务概念不得引入第二套名称：
  - ✅ `apply`、`change`、`read(er)`、`write(r)`、`update(r)`
  - ❌ `derived`、`hydrated`、`mutation`、`read/view/domain model`
- 魔术值收口到常量、枚举或冻结数据对象，不散落在调用点。
- 只有预期且无害的场景才静默忽略异常，并说明为何无害；包装异常保留原始上下文。
- 类、方法、关键变量和关键逻辑存在跨层约束、兼容理由、事务 / 互斥原因或非显然取舍时必须注释；自解释代码不机械复述，字段使用同行尾部注释。
- 可独立观察的业务行为原则上由同目录、同 stem 测试文件负责；跨层契约或集成责任可按行为拆分，纯类型、声明、资源字典、薄组合与生成控件不为文件数量机械配对。
- 应用内 AGENT 模块的内置 skill 是可随时增删改的动态资源，不得在测试中读取或断言其具体名称、数量、顺序、元数据和内容。
- Schema 描述、指令、技能、注释、文档、测试名称与说明以当前事实表达行为和理由。逆向约束或断言仅在实现任务目标确有需要时使用。

### TypeScript / React / CSS

- TypeScript 优先保持显式类型，只有第三方类型确实缺失时才局部使用 `any`。
- React Hook 维护真实依赖数组，不用 eslint 禁用注释掩盖依赖问题。
- 无障碍属性、`alt`、`sr-only` 和 live region 只能复用已有可见文案，不得独占 i18n 键。
- 前端宿主接入、后端 API、用户可见文案和样式消费边界归 [FRONTEND](docs/FRONTEND.md)。

## 3. 维护入口

按消费方确定材料归属，同一任务涉及多类材料时分别采用对应工作流：

|任务对象|归属与维护方式|
|---|---|
|`AGENTS.md`、`.codex/skills/`|开发 Agent 行动规则与技能，仅在任务针对开发工作流时修改|
|`builtin/agent/` 及其加载、测试和消费代码|产品运行时实现，按资源、代码和测试维护；稳定运行边界变化同步 [AGENT_RUNTIME](docs/AGENT_RUNTIME.md)|
|项目声明的长期工程文档|由 [project-doc](.codex/skills/project-doc/SKILL.md) 治理，收录标准适用于这个集合|

长期工程文档完整集合为 `docs/` 下的 [ARCHITECTURE](docs/ARCHITECTURE.md)、[CLI](docs/CLI.md)、[BACKEND](docs/BACKEND.md)、[AGENT_RUNTIME](docs/AGENT_RUNTIME.md)、[FRONTEND](docs/FRONTEND.md) 和 [WORKFLOW](docs/WORKFLOW.md)。专题归宿见 ARCHITECTURE，按需阅读与验证入口见 WORKFLOW。

- 长期文档只记录未来维护必须知道、且不能轻易从代码表面得出的当前稳定事实；同一工程语义在长期文档内只有一个权威归宿。始终适用的关键行动约束在本文件自足表达。
- 改公开协议、状态写入口、数据库、`.lg` 存储、前端运行态、CLI 契约或验证要求时，同一任务内同步对应长期文档。
- 删除或迁移长期文档入口时，更新 README、脚本提示、测试断言、技能提示和其它文档中受影响的引用。
- 文档治理围绕已确认的重复、错位、失效事实和稳定缺口完成；信息归属或组织变化时重组相关段落。现状符合要求时报告依据即可。
- `DESIGN.md` 是 Impeccable 设计流程的可选产物，与 `PRODUCT.md` 等流程产物具有独立生命周期；工程文档的入口与验证应在这些文件缺失时仍然成立。
- 生成物、归档、任务临时材料、外部规范镜像、用户明确保留材料和独立样例按各自生命周期维护；只有符合项目声明范围及收录标准的信息才进入长期工程文档。

## 4. 完成条件

- 修改任务回看 diff，确认命名、注释、实现边界、规则归属与任务范围一致。按 [WORKFLOW](docs/WORKFLOW.md) 的触发条件完成验证；说明执行结果、失败依据和未验证的影响范围。
- 审查、解释、诊断或计划任务以位置、证据、影响和建议交付；证据不足的判断列为未决。
- 前端视觉改动说明采用的当前设计输入或既有视觉证据，以及是否执行真机或等价验证。
- 文档治理按实际发生的删除、合并、迁移、压缩、补写、保留、未决和验证汇报，省略空类别。
- 提交日志遵循 Conventional Commits：`type(scope): 中文摘要 #999[如有]`。
