# LinguaGacha Agent 协作指南

本文件是 LinguaGacha 仓库内 Agent 行动规则的最高指导，始终适用；它不属于项目长期文档体系。`docs/` 只在任务需要理解或修改项目时提供工程事实与维护路径。由于加载时机不同，本文件可以有意重复关键行动约束；若两者对 Agent 行动的要求冲突，以本文件为准，若工程描述与实现冲突，以当前代码事实为准。

## 1. 任务入口

- 先判断任务是审查、解释、诊断、计划还是修改，不把“检查”擅自扩大为“修改”。
- 审查、解释、诊断或计划任务只读取材料并报告结果；用户同时要求改动时再编辑文件。
- 修改、构建或修复任务默认完成端到端工作：阅读、实现、非破坏性验证、回看 diff、说明结果，遇到阻塞先自行排查。
- 需要项目背景、跨层判断或确定验证范围时，按 [`docs/WORKFLOW.md`](docs/WORKFLOW.md) 选择最小必要阅读路径；代码表面即可判定的局部任务可直接读取相关实现和测试，不强制遍历项目文档。
- 外部写入、破坏性操作、付费行为或实质扩大任务范围前必须确认。

技能和指令先按消费方判断归属，不按文件格式、名称或目录表象分类；同一任务涉及不同归属时分别采用对应工作流：

|任务对象|权威归属与处理方式|
|---|---|
|`AGENTS.md`、`.codex/skills/` 等开发 Agent 行动规则与技能|属于开发控制面，只约束如何维护仓库；仅在任务明确针对开发工作流时修改，不视为产品实现或长期工程文档|
|`resource/agent/` 及其加载、测试和消费代码中的产品 Agent 指令与 skill|属于 LinguaGacha 产品运行时实现；按资源、代码和测试维护，稳定运行边界变化时同步 [`docs/AGENT_RUNTIME.md`](docs/AGENT_RUNTIME.md)|
|`docs/` 下的 `ARCHITECTURE.md`、`CLI.md`、`BACKEND.md`、`AGENT_RUNTIME.md`、`FRONTEND.md` 和 `WORKFLOW.md`|构成完整的长期工程文档集合，由 `project-doc` 技能治理|

|需要理解的工程问题|长期文档入口|
|---|---|
|系统分层、跨层边界、运行时主链路、模块关系|[`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)|
|CLI 命令、入口分发、临时工程、资源、输出和平台启动器|[`docs/CLI.md`](docs/CLI.md)|
|后端协议、状态拥有者、唯一写入口、任务、数据库和 `.lg` 存储|[`docs/BACKEND.md`](docs/BACKEND.md)|
|产品 Agent 会话、模型历史、启动资源、skill、工具、宿主能力和页面消费|[`docs/AGENT_RUNTIME.md`](docs/AGENT_RUNTIME.md)|
|Electron、preload、renderer、前端运行态、导航和样式消费|[`docs/FRONTEND.md`](docs/FRONTEND.md)|
|阅读路径、验证矩阵、文档同步和交付自检|[`docs/WORKFLOW.md`](docs/WORKFLOW.md)|

## 2. 协作硬约束

- 不覆盖用户或其他 Agent 的无关改动；工作区脏时只处理本任务涉及的文件。
- 对外部信息、工具链行为、当前依赖版本或网络事实没有把握时，先查证再形成结论。
- 文档与代码冲突时回到当前实现；证据不足列为未决，不写成长期规则。
- 提交日志遵循 Conventional Commits：`type(scope): 中文摘要 #999[如有]`。

## 3. 改动前判定

- 改共享状态、任务、文件集合、数据库或 `.lg` 写入前，确认状态拥有者、唯一写入口、快照形状、事件回流、互斥、事务、失败回滚和补偿语义，不按目录名猜边界。
- 新增状态前明确它属于后端运行态、数据库事实、前端共享运行态、页面缓存还是页面本地状态。
- 改公开协议、状态写入口、数据库、`.lg` 存储、前端运行态、CLI 契约或验证要求时，同一任务内同步对应长期文档。
- 删除或迁移长期文档入口前，确认 README、脚本提示、测试断言、技能提示和其它文档中指向该入口的引用均已更新。
- 新规则若只能靠“补充、另外、注意、同时”塞入现有段落，先重组信息块再落文。
- Schema 描述、指令、技能、注释、文档、测试名称与说明等非逻辑信息，必须从当前事实出发进行正向的描述和指导，避免使用逆向的防御性约束或断言，除非这是实现任务目标所必须的。

## 4. 编码硬约束

通用：

- 命名沿用所在领域的既有词表，同一业务概念不得引入第二套名称：
  - ✅ `apply`、`change`、`read(er)`、`write(r)`、`update(r)`
  - ❌ `derived`、`hydrated`、`mutation`、`read/view/domain model`
- 跨层、跨线程、跨前后端载荷使用显式类型、窄化函数或不可变值对象，不传可变引用。
- 魔术值收口到常量、枚举或冻结数据对象，不散落在调用点。
- 只有预期且无害的场景才静默忽略异常，并说明为何无害；包装异常保留原始上下文。
- 类、方法、关键变量和关键逻辑存在跨层约束、兼容理由、事务 / 互斥原因或非显然取舍时必须注释；自解释代码不机械复述，字段使用同行尾部注释。
- 可独立观察的业务行为原则上由同目录、同 stem 测试文件负责；跨层契约或集成责任可按行为拆分，纯类型、声明、资源字典、薄组合与生成控件不为文件数量机械配对。
- 新增或改变边界规则时优先补行为测试；无法测试时在交付中说明原因和风险。
- 内置 skill 是可随时增删改的动态资源，不得在测试中读取或断言其具体名称、数量、顺序、元数据和内容；skill 机制测试统一使用虚构资源。
- 以“全局最优而非最小改动”“勇于重构和删减，避免过度设计、过度防御和补丁式修改”“重视维护成本、降低结构理解成本”为原则，未经用户确认不引入兼容层。

TypeScript / React / CSS：

- TypeScript 优先保持显式类型，只有第三方类型确实缺失时才局部使用 `any`。
- React Hook 维护真实依赖数组，不用 eslint 禁用注释掩盖依赖问题。
- 无障碍属性、`alt`、`sr-only` 和 live region 只能复用已有可见文案，不得独占 i18n 键。
- 前端宿主接入、后端 API、用户可见文案和样式消费边界归 [`docs/FRONTEND.md`](docs/FRONTEND.md)，验证入口归 [`docs/WORKFLOW.md`](docs/WORKFLOW.md)。

## 5. 文档治理硬约束

- 只有第 1 节声明的长期工程文档集合受 `project-doc` 技能治理；专题归宿见 [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)，开发控制面、产品实现和其它材料不因采用 Markdown、skill 或指令形式而进入该集合。
- 长期文档只记录未来维护必须知道、且不能轻易从代码表面得出的当前稳定事实；同一工程语义在上述长期文档内只允许一个权威归宿。
- Agent 必须始终遵循的关键约束可以在本文件自足表达，不因项目文档已有相近内容而删成仅剩链接。
- `DESIGN.md` 是 Impeccable 设计流程的可选产物，可能不存在；它与 `PRODUCT.md` 等流程产物都不属于工程长期文档，工程文档不得依赖这些文件存在。
- 结构性文档任务必须体现删除、合并、迁移或压缩；只新增内容通常不是完成。
- 生成物、归档、任务临时材料、外部规范镜像、用户明确保留材料和有独立生命周期的样例文件，不自动吸收为长期权威文档。

## 6. 交付硬约束

- 完成后必须回看 diff，确认命名、注释、实现边界、规则层级与文档边界一致。
- 验证按 [`docs/WORKFLOW.md`](docs/WORKFLOW.md) 的矩阵执行；未执行、失败或只执行部分时说明原因与影响范围。
- 前端视觉改动应说明采用的当前设计输入或视觉证据，以及是否执行真机或等价验证；不以 `DESIGN.md` 是否存在作为完成条件。
- 结构性文档任务只汇报信息集合变化：删除、合并、迁移、压缩、补写、保留、未决、验证。
