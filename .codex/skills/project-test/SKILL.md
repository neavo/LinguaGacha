---
name: project-test
description: 新增、修复、审查或整理项目自动化测试，并按仓库现有技术栈选择行为断言、测试替身、文件组织和验证范围。用于单元、集成、契约、UI、回归测试及测试可维护性任务；项目自身约定优先于本技能示例。
---

# Project Test

用最小、稳定的测试证明请求中的可观察行为。先理解真实调用链和项目约定，再选择足以覆盖风险的测试层级。

## 工作流程

1. 判断任务模式：
   - 审查、诊断、解释或计划：读取相关材料并报告，不修改文件。
   - 新增、修复或整理：实施范围内改动，并运行相关的非破坏性验证。
2. 读取项目事实：适用的 `AGENTS.md`、项目文档、测试配置、依赖清单、相邻测试和仓库脚本。
3. 定义目标行为：输入、可观察结果、关键边界或错误分支，以及本次完成标准。
4. 选择最小有效层级：纯规则用单元测试；真实协作、适配器或存储语义用集成测试；跨系统载荷用契约测试；用户交互用 UI 测试。
5. 修复缺陷时，能稳定重现就先写失败回归测试；只整理测试或无法可靠制造失败状态时，不强造红测，记录依据并保持行为不变。
6. 实施最小改动，不顺手扩展范围。确需改善可测性时，只做业务透明重构。
7. 按风险扩大验证：目标测试 → 受影响测试套件 → 仓库已有的 lint、类型或构建检查。
8. 报告改动、实际执行的命令、失败项和未验证范围。

任务模式示例：

| 用户请求 | 允许的动作 |
| --- | --- |
| “审查这些测试是否稳定” | 读取、运行非破坏性检查并报告，不改文件 |
| “修复这个缺陷并补回归测试” | 先稳定复现，再修改实现、测试并验证 |
| “整理这批旧测试” | 保持业务行为，修改、合并、拆分或删除有明确依据的用例 |

## 核心判断

- **断言结果**：优先检查返回值、错误、公开状态、事件、持久化结果、DOM 或公开回调；调用次数和参数只在它们本身构成契约时断言。
- **选择测试替身**：优先运行快速、确定且无副作用的真实逻辑。对远程服务、时间、随机数、系统接口、昂贵资源或会干扰测试目的的协作者使用 fake、stub 或 mock；不要只按“仓库内外”机械决定。
- **隔离副作用**：除非用户和项目明确提供测试环境，不访问真实用户目录，不向真实外部服务写入，不依赖测试顺序或残留状态。
- **组织文件**：遵循仓库现有约定。让同一行为的测试易于发现；按测试层级、场景边界或文件规模拆分确有助于理解时可以拆分，避免重复覆盖同一行为的平行测试。
- **表达业务意图**：测试名和数据直接说明需求、边界或历史缺陷；不要为覆盖率数字制造孤儿用例。
- **保持可维护性**：夹具和 helper 放在最小共享范围；不暴露私有实现只为方便测试；不通过削弱断言让失败消失。

## 完成与停止条件

完成至少意味着：

- 请求中的行为或审查目标已有明确证据；
- 修改后的目标测试通过，相关回归范围与风险匹配；
- 没有未说明的业务语义变化或无关改动；
- 未执行的验证及原因已经报告。

遇到以下情况时收敛处理：

- 关键业务要求无法从代码、测试或文档推断时，只询问最小阻塞信息。
- 项目命令或依赖不可用时，先寻找仓库内等价入口；不要为了套用示例擅自新增依赖。
- 外部文档确有必要时，使用当前环境可用的官方文档工具，并核对项目实际依赖版本。
- 审查旧测试时，不因“看起来不可达”直接删除；先确认公开入口、历史缺陷和风险价值。

## 参考文件路由

只读取当前任务需要的参考文件：

| 任务 | 参考文件 |
| --- | --- |
| Python、pytest、文件系统、数据库、并发 | [references/python-pytest.md](references/python-pytest.md) |
| TypeScript、Vitest、状态或异步逻辑 | [references/typescript-vitest.md](references/typescript-vitest.md) |
| React、Hook、Context、组件交互 | [references/react-renderer.md](references/react-renderer.md) |
| API、事件、进程、前后端或宿主边界 | [references/cross-stack-boundaries.md](references/cross-stack-boundaries.md) |
| 审查、合并、清理或重构旧测试 | [references/test-audit.md](references/test-audit.md) |
| 定位白盒断言、mock 滥用和测试坏味道 | [references/anti-patterns.md](references/anti-patterns.md) |

若技术栈不在表中，先识别仓库已有测试框架和命令，再按同样的行为、隔离与验证原则工作；不要套用无关示例。
